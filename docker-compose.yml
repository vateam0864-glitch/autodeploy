version: "3"

services:
  app1:
    build:
      context: ./app1
      dockerfile: Dockerfile
      args:
        CACHEBUST: ${CACHEBUST}
    ports:
      - "5001:5001"
    restart: unless-stopped

  app2:
    build:
      context: ./app2
      dockerfile: Dockerfile
      args:
        CACHEBUST: ${CACHEBUST}
    ports:
      - "5002:5002"
    restart: unless-stopped
